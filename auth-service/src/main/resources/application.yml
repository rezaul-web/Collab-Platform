server:
  port: 8081

spring:
  application:
    name: auth-service

  # ── PostgreSQL ──────────────────────────────────────────────────────
  datasource:
    url: jdbc:postgresql://localhost:5432/collab_auth
    driver-class-name: org.postgresql.Driver
    username: postgres
    password: Rezaul@2004

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect

grpc:
  server:
    port: 9091

app:
  jwt:
    secret: Y29sbGFiLXBsYXRmb3JtLXN1cGVyLXNlY3JldC1rZXktMTIzNDU2Nzg=
    expiration-ms: 86400000

logging:
  level:
    com.example.collab.auth: DEBUG
